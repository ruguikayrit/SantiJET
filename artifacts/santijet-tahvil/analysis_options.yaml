# ŞantiJET Tahvil — Demir referans projesiyle aynı lint yapılandırması.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "build/**"
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**

linter:
  rules:
