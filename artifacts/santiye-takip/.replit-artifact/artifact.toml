kind = "mobile"
previewPath = "/"
title = "ŞantiJET"
version = "1.0.0"
id = "artifacts/santiye-takip"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 24914

[services.development]
run = "pnpm --filter @workspace/santiye-takip run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/santiye-takip", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/santiye-takip", "run", "serve" ]

[services.env]
PORT = "24914"
BASE_PATH = "/"

[[services]]
name = "web"
paths = [ "/app" ]
localPort = 24915

[services.development]
run = "pnpm --filter @workspace/santiye-takip run dev:web"

[services.env]
PORT = "24915"
BASE_PATH = "/app"
